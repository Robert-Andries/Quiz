<UserControl x:Class="Quiz.WPFUI.Views.QuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="750"
             FontSize="20">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding QuestionNumber}"
                   Grid.Row="1" FontSize="30"
                   HorizontalAlignment="Center" />

        <TextBlock Text="{Binding QuestionText}"
                   HorizontalAlignment="Left"
                   TextWrapping="Wrap"
                   Grid.Row="2"
                   Margin="40, 20, 40, 40" />

        <ItemsControl ItemsSource="{Binding QuestionsDtos}" Grid.Row="3" Margin="40, 0, 40, 20">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" Margin="0, 10, 0, 10"
                              VerticalContentAlignment="Center">
                        <CheckBox.Content>
                            <TextBlock Text="{Binding OptionText}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                        </CheckBox.Content>
                    </CheckBox>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Content="Next Question"
                MaxHeight="40" MaxWidth="200"
                FontSize="20" HorizontalAlignment="Right"
                Margin="20, 0"
                Grid.Row="5"
                Command="{Binding NextQuestionCommand}" />
    </Grid>
</UserControl>